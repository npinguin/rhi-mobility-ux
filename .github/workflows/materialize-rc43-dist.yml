name: Validate and materialize rc43 dist
# rerun after UX contract-test correction

on:
  push:
    branches:
      - ux/rc43-status-simplification
    paths:
      - .github/workflows/materialize-rc43-dist.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: ux/rc43-status-simplification
          fetch-depth: 0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run clean
      - run: npm run build
      - name: Commit deterministic dist
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dist
          if git diff --cached --quiet; then
            echo "dist already current"
            exit 0
          fi
          git commit -m "Materialize rc.43 deterministic dist"
          git push origin HEAD:ux/rc43-status-simplification
