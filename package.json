{
  "name": "rhi-mobility-ux",
  "version": "1.0.0-rc.3",
  "private": false,
  "description": "Robotix Home Intelligence Mobility UX for Home Assistant",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "clean": "node tools/clean.mjs",
    "build": "node tools/build.mjs",
    "check:syntax": "node --check dist/rhi-mobility-ux.js",
    "check:architecture": "node tools/check-architecture.mjs",
    "check:hygiene": "node tools/check-public-hygiene.mjs",
    "test:contracts": "node tests/contracts/runtime-contract-smoke.mjs && node tests/contracts/command-placement-smoke.mjs",
    "test:viewmodels": "node tests/view-models/charger-materialization.mjs",
    "test:render": "node tests/render/bundle-load-smoke.mjs",
    "test": "npm run clean && npm run build && node tools/check-version.mjs && npm run check:syntax && npm run check:architecture && npm run check:hygiene && npm run test:contracts && npm run test:viewmodels && npm run test:render && node tools/verify-dist.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
